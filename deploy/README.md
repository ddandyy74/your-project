# deploy/ - 部署配置目录

> **备注**: deploy/ 🚀 （如果有代码）部署脚本/配置（DEV 写，OPS 只读执行）
> 部署脚本、配置文件、部署说明

## 目录内容

```
deploy/
├── scripts/          # 部署脚本
├── configs/          # 配置文件
├── docker/           # Docker 相关
└── k8s/              # Kubernetes 配置
```

## 使用说明

### 部署脚本

```bash
# 部署到开发环境
./deploy/scripts/deploy.sh dev

# 部署到预发布环境
./deploy/scripts/deploy.sh staging

# 部署到生产环境
./deploy/scripts/deploy.sh production
```

### 回滚脚本

```bash
# 回滚到上一版本
./deploy/scripts/rollback.sh

# 回滚到指定版本
./deploy/scripts/rollback.sh v1.0.0
```

## 部署流程

1. 构建产物 → `dist/` 或 `build/`
2. 镜像构建 → Docker
3. 服务部署 → K8s / 脚本
4. 健康检查 → 验证部署成功

## 注意事项

- OPS 只读执行，不修改此目录
- 生产环境部署需遵循 [08_deploy_plan.md](../docs/08_deploy_plan.md)
- 敏感配置通过环境变量注入，不写入此目录
